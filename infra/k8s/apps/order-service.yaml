apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: saganet
  labels:
    app: order-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: ghcr.io/saganet/order-service:latest
          ports:
            - containerPort: 3004
          envFrom:
            - configMapRef:
                name: saganet-config
            - secretRef:
                name: saganet-secrets
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: saganet
spec:
  selector:
    app: order-service
  ports:
    - port: 3004
      targetPort: 3004
