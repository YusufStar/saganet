apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: saganet
  labels:
    app: payment-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: ghcr.io/saganet/payment-service:latest
          ports:
            - containerPort: 3005
          envFrom:
            - configMapRef:
                name: saganet-config
            - secretRef:
                name: saganet-secrets
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3005
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3005
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: saganet
spec:
  selector:
    app: payment-service
  ports:
    - port: 3005
      targetPort: 3005
